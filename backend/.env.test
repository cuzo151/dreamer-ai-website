# Test Environment Variables
NODE_ENV=test
PORT=5001

# Test Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dreamer_ai_test
DB_USER=postgres
DB_PASSWORD=postgres

# Test Redis Configuration
REDIS_URL=redis://localhost:6379/1

# JWT Secrets (Test only - DO NOT USE IN PRODUCTION)
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_REFRESH_SECRET=test-jwt-refresh-secret-for-testing-only

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:3000

# Email Configuration (Mock for testing)
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_USER=test@ethereal.email
EMAIL_PASS=test-password
EMAIL_FROM=noreply@dreamer-ai-test.com

# Rate Limiting (More permissive for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Security
CORS_ORIGIN=http://localhost:3000
SESSION_SECRET=test-session-secret-for-testing-only

# Logging
LOG_LEVEL=error